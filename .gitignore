# 依赖
node_modules/

# TS 编译产物(由 src 生成,不入库)
mind_tap/game/dist/

# 工作区内部数据
.workbuddy/

# 系统/日志
*.log
.DS_Store
Thumbs.db

# 微信开发者工具个人私有配置(不入库)
project.private.config.json

# 音色调参试听产物(游戏内实时合成,无需入库)
mind_tap/*.wav

# mind_tap 根目录冗余工程配置(实际工程在 mind_tap/game/)
mind_tap/project.config.json
